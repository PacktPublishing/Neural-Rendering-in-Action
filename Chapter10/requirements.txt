# Core dependencies for the companion notebook to Chapter 10 -- Generative Models for
# Graphics: A Diffusion Primer.
#
# On Google Colab, torch/torchvision/numpy/matplotlib/scikit-learn are already
# preinstalled, and the notebook installs the remaining packages itself in its first
# cell (%pip install ...). This file is for reproducing the notebook OUTSIDE Colab,
# e.g. on your own machine with a GPU.

torch>=2.1
torchvision>=0.16
diffusers>=0.31
transformers>=4.46
accelerate>=1.0
safetensors>=0.4
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.24
Pillow>=10.0
nbformat>=5.9
